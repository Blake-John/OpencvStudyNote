cmake_minimum_required(VERSION 3.0)
project(DETECTC)
find_package(OpenCV REQUIRED)
add_executable(detectc detectc.cpp)
target_include_directories(detectc PUBLIC ${OpenCV_INCLUDE_DIRS})
target_link_libraries(detectc ${OpenCV_LIBS})