cmake_minimum_required(VERSION 3.0)
project(DETECTS)
find_package(OpenCV REQUIRED)
add_executable(detects detects.cpp)
target_include_directories(detects PUBLIC ${OpenCV_INCLUDE_DIRS})
target_link_libraries(detects ${OpenCV_LIBS})